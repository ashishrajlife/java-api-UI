// Initialize AOS animations (if you're using AOS library for scroll animations)
document.addEventListener('DOMContentLoaded', function () {
    AOS.init({
      duration: 1200,
      once: true, // Animations will only happen once
    });
  });
  